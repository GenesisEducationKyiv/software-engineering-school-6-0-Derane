# Expected PR checks for the code-review skill final readiness gate.
# Format: type|name|source
# Names must match the GitHub check-run names produced by .github/workflows/*.yml
# (each entry is a job `name:` in those workflows). Tune after the first PR run
# if GitHub reports a different context string.
check_run|PHP_CodeSniffer & Psalm|github-actions
check_run|deptrac (bounded-context boundaries)|github-actions
check_run|PHPUnit|github-actions
check_run|PHPUnit (notification service)|github-actions
check_run|PHPUnit (real Postgres + Redis)|github-actions
check_run|PHPUnit (real Postgres + RabbitMQ + MailHog)|github-actions
check_run|Behat (HTTP contract)|github-actions
check_run|Behat (auth enabled)|github-actions
check_run|Playwright (browser)|github-actions
check_run|Playwright (auth enabled)|github-actions
check_run|scanner-smoke + AC5 / NFR3 outage recovery|github-actions
check_run|composer audit|github-actions
